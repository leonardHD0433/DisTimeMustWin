For conda:
        
    conda env create -f requirements.yml

For venv:

    python -m venv myenv  # replace 'myenv' with the name of your environment
    source myenv/Scripts/activate  # On Windows
    pip install -r requirements.txt